<?xml version="1.0" encoding="utf-8"?>
<!--
  Exposes the app's cache directory (where path_provider's
  getTemporaryDirectory() writes the on-device Quotation/Estimate/Receipt
  PDF) so FileProvider can hand WhatsApp a content:// uri for it.
-->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="ncw_shared_pdfs" path="." />
</paths>